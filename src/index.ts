/**
 * dsh-smooth-scroll — host half.
 *
 * Pure UI plugin: the empty apply exists so the plugin appears in the host
 * composition; the browser half ships via exports["./client"], discovered
 * through the package.json `dsh.client` declaration (same pattern as
 * liangwengu).
 */
function apply() {
}

export { apply }
